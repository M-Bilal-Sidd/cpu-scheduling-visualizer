CPU Scheduling Visualizer
Operating Systems Project

REQUIREMENTS TO RUN:
This project uses the CustomTkinter library for its modern graphical interface. 

To run the application, please open your terminal or command prompt in this folder and run the following commands:

1. Install the required UI library:
pip install -r requirements.txt

2. Run the application:
python app.py